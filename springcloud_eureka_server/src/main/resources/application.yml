eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
# 纯粹的服务注册中心
    fetch-registry: false
    register-with-eureka: false

server:
  port: 8761

spring:
  application:
    name: eureka